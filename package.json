{
  "name": "bookbook",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @bookbook/web"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
